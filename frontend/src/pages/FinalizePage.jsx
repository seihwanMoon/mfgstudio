import PlaceholderPage from "./PlaceholderPage"

export default function FinalizePage() {
  return <PlaceholderPage title="모델 확정" description="finalize_model(), save_model(), MLflow Registry 화면입니다." />
}
